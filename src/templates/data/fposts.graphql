query {
  getFeaturedPostList(sort: [{field: "_enabledAt", order: "desc"}]) {
    total
    items {
      _contentTypeName
      _enabledAt
      _createdAt
      title
      deck
      location {
        name
      }
      linkUrl
      tags {
        name
      }
      author {
        name
        image {
          path
        }
        locationaut {
          name
        }
      }
      featuredImage {
        path
      }
      bodyHtml
    }
  }
}
